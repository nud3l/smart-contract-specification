```
behaviour getExample of SimpleStruct
interface getExample(uint Id)

types

    Someone   : address
    Somevalue : uint256
    Check     : bool

storage

    #hashedLocation("Solidity", 0, )

if
    VGas >= 100000
```
